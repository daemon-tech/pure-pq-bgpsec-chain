--- a/usr.sbin/rpki-client/extern.h
+++ b/usr.sbin/rpki-client/extern.h
@@ -XX,XX +XX,XX @@
 #define OID_ECDSA_P256		"1.2.840.10045.4.3.2"
 #define OID_ECDSA_P384		"1.2.840.10045.4.3.3"
+/* Falcon-512 signature algorithm OID (post-quantum) */
+#define OID_FALCON512_SIG	"1.3.9999.3.1"
 
--- a/usr.sbin/rpki-client/validate.c
+++ b/usr.sbin/rpki-client/validate.c
@@ -XXX,XX +XXX,XX @@ valid_signature_algorithm(const char *alg)
+	/* Falcon-512 (post-quantum) */
+	if (strcmp(alg, OID_FALCON512_SIG) == 0)
+		return (1);
+
 	/* ECDSA P-256 and P-384 */
 	if (strcmp(alg, OID_ECDSA_P256) == 0 ||
 	    strcmp(alg, OID_ECDSA_P384) == 0)
